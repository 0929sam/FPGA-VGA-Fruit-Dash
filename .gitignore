# Quartus compilation output / generated cache (regenerate with Quartus, don't commit)
db/
incremental_db/
output_files/
simulation/
*_sim/
*.qws
*.rpt
*.summary
*.smsg
*.sld
*.pin
*.jdi
*.done
*.qarlog
*.htm
c5_pin_model_dump.txt

# Editor / backup files
*.bak
*~

# OS junk
.DS_Store
Thumbs.db

# Raw phone footage over ~100MB (GitHub's hard file-size limit) — trim/compress before adding
IMG_8431.MOV
